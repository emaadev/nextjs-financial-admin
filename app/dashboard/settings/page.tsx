import { PageHeader } from "@/components";

export default function SettingsPage() {
  return (
    <section className="settings-page">
      <PageHeader
        title={"Settings"}
        subtitle={"Change the config of your account."}
      />
    </section>
  );
}
